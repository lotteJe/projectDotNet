@model KostenBatenTool.Models.AnalyseViewModels.OpleidingsKostViewModel
@{
    ViewData["Title"] = "5. Extra kosten opleiding";
}

<h1>@ViewData["Title"]</h1>
<hr />
<h2></h2>

<div class="row">
    <h2 class="col-md-7">Welke opleidingskosten moeten er gemaakt worden om de nieuwe kraachten op het gewenste niveau te krijgen?</h2>
    <a id="toevoegenFunctie" class="col-md-offset-4"><i class="fa fa-plus fa-2x  rotate" aria-hidden="true"></i></a>
</div>

<div class="row">
    <div class="col-md-6 ">
        <table class="table table-responsive table-striped functies">
            <tr>
                <th>Type</th>
                <th>Bedrag</th>
            </tr>
            <tr>
                <td>-</td>
                <td>-</td>
            </tr>
            @*@foreach (var item in Model)
                {

                    <tr>
                        <td>@item.Type</td>
                        <td>@item.Bedrag</td>

                    </tr>
                }*@
        </table>
    </div>
    <div id="partial" class="col-md-6">
        @Html.Partial("_opleidingsKostPartial")
    </div>
</div>